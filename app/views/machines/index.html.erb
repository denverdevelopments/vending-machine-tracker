<h1>Vending Machines</h1>

<% @machines.each do |machine| %>
<p><%= link_to machine.location, machine_path(machine)%></p>
<p><%= link_to machine.location, kevscrazyroad_path(machine)%></p>
<p><%= link_to machine.location, "/machines/#{machine.id}"%></p>
<% end %>
